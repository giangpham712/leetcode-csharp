using Common;

namespace Algorithms.Tree;

public class InorderTraversal
{
    public List<TreeNode> Traverse(TreeNode root)
    {
        var traversed = new List<TreeNode>();

        return traversed;
    }
}