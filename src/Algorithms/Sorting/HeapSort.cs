namespace Algorithms.Sorting;

/// <summary>
/// - Build a max heap from the input data. <br/> 
/// - At this point, the maximum element is stored at the root of the heap.
/// Replace it with the last item of the heap followed by reducing the size of the heap by 1. Finally, heapify the root of the tree. <br/> 
/// - Repeat step 2 while the size of the heap is greater than 1. <br/>
/// </summary>
public class HeapSort
{
    public int[] Sort(int[] arr)
    {
        
        
        return arr;
    }
}